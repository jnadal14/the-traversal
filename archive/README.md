# Landing page archive

`landing-mobile-before-rebuild-2026-07-30.html` is the complete landing page
captured immediately before the mobile carousel rebuild. The previous mobile
scroll implementation was embedded in `index.html`, so the full page is kept
here as a restorable snapshot.
